# Sample Spring Batch application

This application converts age to DOB for data in resources/sample-data.csv.
It then stores data into mysql server.
It also includes docker file for running mysql db.

To run:
- docker-compose up -d
- Run application with Intellij